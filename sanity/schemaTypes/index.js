import { projectType } from './projectType';
import { homeType } from './homeType';
import { workType } from './workType';
import { aboutType } from './aboutType';

export const schema = {
  types: [homeType, workType, aboutType, projectType],
};
